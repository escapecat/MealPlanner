# 进度日志

> 每轮工作追加一条。最新的在最上面。
> 早上起来看这个文件就行,不用翻聊天记录。

---

## 2026-08-04 上午 · 应用层开工 —— 骨架 + 冷启动 + 包装规格可编辑

**双击 `app/index.html` 就能跑**,不用起服务器。

### 做了什么

| | |
|---|---|
| `tools/build_data.py` | markdown → `app/data/*.js`。628 食材 · 535 菜谱(614 variants)· 135 包装,零悬空 |
| `app/lib/store.js` | 存储抽象。迁小程序时**只有这一个文件要改** |
| `app/core/profile.js` | Mifflin-St Jeor + 饭量护栏。纯函数,能在 node 里直接测 |
| `app/core/catalog.js` | 从菜谱库**推导**厨具/味型候选池 + 按配置过滤 |
| `app/ui/onboarding.js` | 冷启动:身体数据 6 项 + 配置 5 项,一屏滚完 |
| `app/ui/packages.js` | 包装规格:默认值 + 用户就地编辑 |
| `app/style.css` | 配色**全走 CSS 变量**,深色模式第一天就在 |

### 两个设计决定

**厨具清单不硬编码,从菜谱库推。** 数据层那条「能推导的不重复存」原则同样适用于 UI —— 库还在长,硬编码的勾选项迟早对不上。附带好处:冷启动时能实时显示可做菜数,取舍当场看得见。

| 厨具 | 可做 |
|---|---|
| 只有炒锅 | 153 / 512 |
| 炒锅 + 汤锅 | 280 |
| 再加不粘锅 + 蒸架 | 393 |
| 全套九样 | 512 |

**包装规格:生成值和用户值分开存。** `PACKAGES` 是默认值,重新生成会覆盖;用户改的存在 `Store.packageOverrides`,**永远不被覆盖**,且自动标成可信度 A。这样数据层能继续演进,而用户在超市站着改的那几个数不会丢。

### ⚠️ 我自己写了一个静默损坏,和昨晚抓的那三条同类

转换器第一版把「20**只**速冻水饺」解析成 **20 克** —— 数字抠对了,单位丢了。全库 200 处按件计量的(鸡蛋 2 个、饺子皮 25 张、吐司 2 片)全部变成克。**不报错、不崩溃,只是让求解器照着买 20 克饺子。**

修法还是那条:**如实保留,不要伪造**。现在 `qty` / `unit` / `grams` 分三个字段,`鲫鱼 1条 300g` 两个都记。换算是字典的事,转换层不假装知道。

### 字典补了「单位换算」列 —— 52 → 5

转换器现在会**主动报出它算不了的东西**。补完之后只剩 5 条,而且**每一条都写了为什么算不出**:

```
kaffir_lime_leaf  片×6  ← 一片不到 1g 且大小差三倍,按「适量」处理更靠谱
puff_pastry       张×1  ← 市售从小方块 30g 到大张 250g,包装只标张数不标克重,无锚点
bay_leaf          片×1  ← 一片约 0.2g,量级太小换算无意义
pandan_leaf       片×1  ← 叶片 2-8g,用量又极小
rice_wrapper_gz   张×1  ← 新建条目,规格待核实
```

**鸡蛋那条(70 处,最重要)的值不是编的**:从它自己的「常见包装 10枚/盒 500g」反推出 `1个≈50g`。如果主值填 45g(去壳),2 个蛋 = 90g ÷ (500g/10枚) = **1.8 枚**,取整还是 2 枚 —— 答案一样但中间那个 1.8 是假的,用户一改包装规格就会漏。去壳 45g 写进括号,算营养时取。

顺带修了两处:`dried_chili` 字典写「1个≈1g」而 RH-028 写「1根」,量词不通 → 字典两个都收;`米皮[?]` 建成 `rice_wrapper_gz`,**规格留空但 id 是真的** —— 悬空引用会让求解器崩,有 id 才进得了采购清单。

### 🔴 与 DESIGN.md 的一处矛盾(按约束只记录,没改)

`DESIGN.md` 第八节「关键决策记录」有一行:

> **开工时机 | 先手工跑 3-4 周 | 现在没有真实克数/耗时/饭量,建库只能编**

这条已被推翻(「不 我喜欢先设计好 再实践修改」),而且 535 道菜已经建完。**等你定**:删掉,还是改成「~~先手工跑 3-4 周~~ 已推翻:先设计后实践,库已建成但 `verified: false`」?

我倾向后者 —— 保留改过主意的记录,和那张表里 ~~靠反馈学饭量~~、~~照搬麻将称号~~ 的写法一致。

### 下一步

按依赖,**#10 求解器**卡在两件事上:包装规格没人核对过(99.3% 未验证)、库存模块还没有。建议先做 **#9 储物柜** + **#11 库存**,把求解器的输入凑齐。

---

# ✅ 数据层已完成(2026-08-04 04:51)

**2026-08-04 04:51** · 24 个 git 提交 · 校验器 `python tools/validate.py` **错误 0 · 告警 0 · 提示 0**

| | 数量 | |
|---|---|---|
| 菜谱 | **535 道** | 01=121 · 02=86 · 03=77 · 04=78 · 05=78 · 06=95 |
| `prepLevel` 变体 | **79 条** | 01=4 · 02=11 · 03=17 · 04=22 · 05=9 · 06=16 |
| 食材字典 | **627 条** | 动物性 137 · 蔬菜菌菇 124 · 主食干货调料 316 · **预制成品 50** |
| 包装规格 | 135 条 | `data/packages.md` |

**四个字典的过滤字段口径已统一**:含酒列全部存在、位置一致(过敏原之后)、取值只有 `是`/`否`、无缺格。含酒=是 共 39 条(01=3 · 02=0 · 03=20 · 04=16)。

## ⚠️ 用之前必须知道的三件事

### 1. 全库未验证

**所有克数、耗时、难度都是估计值,没有一道实际做过并称量过。** `verified: false`

- `packages.md` 的包装规格 **99.3% 未经原文核实**(A 级只有 1 条)
- `04-预制成品.md` 50 条:可信度 **A=0 / B=2 / C=48**,保质期 47 条留空,参考价 40 条留空
- **含酒列是推断值**,没有实物配料表,四个文件里各自标注了「须人工校准」

求解器要精确算「剩余量」,输入不准的话算出来的「零浪费」是假的。**所以「默认值 + 用户就地编辑」不是锦上添花,是这一层能不能用的前提。**

### 2. 校验器拦不住的一类 bug

`tools/validate.py` 检查:道数 · 列数 · 悬空 id · 两列重复 · 名称位/克数位含括号 · 静默漏解析 · 菜名含主食字样却没有主食角色 · 显示名不在字典别名列里。

**但它拦不住「语法完全合法,只是买错东西」。** 今晚抓到三条,全部是人读出来的:

| 写的 | 实际该买的 |
|---|---|
| `卤蛋[egg]` | 生鸡蛋 → 卤蛋是熟食成品 |
| `腌黄瓜[pickled_cucumber]` | 酸黄瓜 gherkin(汉堡用)→ 台式卤肉饭要的是**台式脆瓜** |
| `即食燕麦[rolled_oats]` | 别名把两个 SKU 收成了一条 |

发现方法只有一个,已写进 SPEC:**拿着菜谱去字典里读那条的「常见包装」和「备注」,问自己「照这个去货架上拿,拿到的是这道菜要的东西吗」。**

### 3. 三条留给你裁决的

- **RH-074 banh mi `烤猪肉[pork_shoulder] 120g`** —— 可能是熟重(该换算成生重 185g),也可能作者本意就是「120g 生肉拿去烤」。歧义没消除,没动
- **`pickled_gourd_tw` 台式脆瓜**的规格/冷藏天数/营养/成本档全部留空 —— 没有依据,不许编
- **不建 `instant_oats`** 是 agent 的判断(全库没有一道菜真需要即食燕麦),理由写在 `rolled_oats` 备注里,反悔成本很低

## 这一夜学到的

**同一个错误犯了六次:按行套正则,而不是按表解析。**

附录表被数进主表(01 报 164 道实际 121)· `|` 检测匹配到 markdown 列分隔符(报 119 处实际 0)· 「海**盐**」后缀误命中导致我错并 `sea_salt`(125g 盒装马尔顿盐 vs 400g 袋装炒菜盐,**包装差一个数量级**)· 内层循环变量遮蔽(报「121 道应为 2」)· **信了 agent 报告里的列位没核**(「三种醋含酒矛盾」根本不存在,那个 `是` 在「必然过量」列)· 临时统计脚本又把附录行数进去(672 vs 627)。

`tools/validate.py` 就是为了终结这件事写的 —— 先把 markdown 切成一张张表、按表头签名分类,再在结构上检查。**但第六次是在它写好之后犯的**,因为我图快又拼了个临时脚本。

**判据从数字演化到可复述的规则。** 变体表的起点是我按「难度≥4 或活跃≥40」扫的 82 道,最后收敛成三条全部满足:①采购清单真的变成另一样东西 ②货架上认得出的独立 SKU 不是料理包 ③scratch 档本身构成障碍。第三条是用来替换「收益太小」的 —— **不可证伪的理由必然跨文件分裂**,曾经同一道红烧肉,01 给了变体、03 按「像凑数」撤掉了。

**agent 推翻我七次,每次理由都更硬。** 干炒牛河不需要 assembled(买半成品在主表里已是默认写法)· 惠灵顿主表本来就写着酥皮 · 复制酱油买不到成品而六道菜全卡在它上面 · `调料` 列格式本来就不带角色位 · 不建 `instant_oats`(全库没菜真需要)· 叉烧/卤牛肉判不含酒(卤 1-2 小时加热 vs 醉蟹生泡不加热)· **三种醋的「是」我看串列了**。

**并发纪律。** 六个菜谱文件全程各一个写者,裁决通过 SendMessage 回发给**该文件自己的** agent,不新派人。04 收到「05 也有同类问题」时拒绝越界只请我转手 —— 那正是上次事故的直接原因。跨文件操作(id 回填、字典合并)全部等到所有单文件写者终止后由**单一 agent** 做。

---

## 2026-08-04 03:30 · 第 14 轮 —— `prepLevel` 变体六路完成,79 条

### ✅ 交付

**535 道一道没少,79 条变体**:01=4 · 02=11 · 03=17 · 04=22 · 05=9 · 06=16。
校验器零错误。同时收掉的还有:02 的 37 处两列重复 + 57 道香料补录(它是唯一漏了这两项的文件)、11 处名称含括号、7 处克数位含括号、5 道菜缺主食。

### 🔧 写了校验器,不再靠临时 grep

这是第四次做数据检查,**前三次全被正则骗了**(附录表数进主表、`|` 检测匹配到列分隔符、「海**盐**」后缀误命中导致我错并 `sea_salt`)。根因一样:**按行套正则,而不是按表解析**。

`tools/validate.py` 先把 markdown 切成一张张表、按表头签名分类,再在结构上检查。第一次运行就抓到 **02 有 37 处「食材/调料 两列重复」** —— 这个 bug 会让采购量直接翻倍。其余五个文件早先都清过,唯独 02 一处没清,和它「唯一没补香料」同一个原因:**两次清理都发生在它被并发事故毁掉、还没恢复的那段时间里**。一个文件连漏两项不是巧合,是那次事故的余震比我以为的长。

它还查出 **4 个丼饭全都没有米饭**(RH-001/002/003/004)—— 这类漏眼睛看不出来。

### ⚠️ 但工具的覆盖面还是人定的

- **06 比校验器准**:它自查出 `RH-015 咖喱牛肉饭` 也缺米饭,而我的关键词表为了避免误报只收了「盖饭/拌饭/炒饭」这些组合词,**漏了光杆的「饭」**。补上之后又误报了「电**饭**煲卤鸡腿」,只好再加负向断言
- **02 抓到校验器的静默漏检**:第 37 处 `外婆菜(袋装)[waipocai]` 没被扫出来,因为括号破坏了名称正则,解析器把**整项静默丢弃**了。静默丢弃比报错危险得多 —— 已加检查:`[id]` 总数对不上解析出的项数就报错
- **我自己写的变量遮蔽**:内层循环的 `want` 覆盖了外层「应有道数」,一度报「121 道应为 2」

### 判据从「难度数字」演化到三条,全部满足才给

起点是我按「难度≥4 或 活跃≥40」扫出的 82 道。这个阈值只能当起点:

1. **采购清单真的变成另一样东西** —— 01 拒回锅肉的理由:买切好的五花肉片,**id 还是 `pork_belly`,清单一个字不变**,这行变体没有信息量
2. **是货架上认得出的独立 SKU,不是通用料理包** —— 一旦承认料理包,每道炖菜都能配一行,那就是凑数
3. **scratch 档本身构成障碍** —— 用来替换「收益太小」这种**不可证伪**的说法。曾经同一道红烧肉,01 给了变体、03 按「像凑数」撤掉了,**库不自洽求解器的行为就是任意的**

⚠️ 判据③ 活跃和总分钟都算:红烧肉活跃只有 20 分钟,但总分钟 80 占掉整个下午。

### agent 推翻我的四处,理由都成立

- **03**:本文件没有 `assembled` 是结论不是遗漏 —— 买半成品在主表里**已经是默认写法**(鲜河粉/烧卖皮/燕皮),再编一档会和主表逐字相同
- **05**:惠灵顿不给 —— 主表本来就写 `酥皮[puff_pastry]`,已经是买冷冻酥皮了
- **02**:复制酱油不给 —— 字典两处写死「买不到成品」,而**六道菜全卡在它上面**,这是唯一一个花钱也绕不过去的味型开关
- **04**:`花椒[sichuan_pepper] 适量` 不带「调料」二字 —— 它查了实际数据,发现 `调料` 列格式本来就不带角色位,加上去会造出全库唯一的异类

### 05 提的一条通则已写进 SPEC

**「或」组的两侧必须是同一档位。** `派皮 或 低筋面粉` 是跨档位的,那是 `prepLevel` 要表达的东西 —— 留在主表则变体表冗余,**两张表会打架**。

### 🔧 成品该不该进字典:收

字典的「已知未收录」把速冻饺子判为「复合成品,不是单一食材」拒收,而 SPEC 要求成品必须有 id —— 两边对冲。

裁决是收,但理由不是「SPEC 说了算」:**字典的职责不是「原子食材」,是「你会买的东西」**。速冻虾饺没有 id 就没有包装规格、没有保质期、没有成本档,`readymade` 那一档对求解器直接不可解。「单一食材」是分类学洁癖,「一个 SKU」才是这个应用需要的粒度。

但成品是另一类东西(按件不按 100g、营养复合、还带「牺牲了什么」),所以另开 `04-预制成品.md`,不污染现有三个。约 50 条,六路已按统一格式登记好规格/温区/价位。

### 并发纪律

六个文件各**一个**写者,全程没有第二个 agent 碰过同一个文件。裁决通过 SendMessage 回发给**该文件自己的** agent,不新派人。04 收到「05 也有同类问题」时**拒绝越界**,只是请我转手 —— 那正是事故的直接原因(有 agent 在别人的文件上写字)。

### 下一步
1. 字典 agent 在跑:建 `04-预制成品.md` + 清 31 个跨字典重复 id + 4 对异 id 同物
2. 然后**单一 agent 跨文件回填**六个变体表里的 `[?]` —— 必须等所有单文件写者终止
3. 数据层就剩这两项

---

## 2026-08-04 凌晨 · 第 13 轮 —— 字典拆分落地,`prepLevel` 变体开工

### ✅ 字典层去重与拆分完成:601 → 612 条

- **拆 6 组 → 7 个新 id**:`pangasius`(巴丁鱼,和鲳鱼不是一回事)· `holy_basil`(打抛叶 ≠ 九层塔)· `houjicha`(焙茶 ≠ 煎茶)· `vodka`(≠ 白酒)· `pork_blood`(≠ 鸭血)· `white_kidney_bean` + `red_kidney_bean`(原来挤在 `kidney_bean` 一条里)
- **补缺 5 条**:`gastrodia`(天麻)· `foxtail_millet` · `apple_juice` · `jalapeno` + `pickled_jalapeno`
- **补别名 5 个** · **收窄别名 4 处** · **删重复条目 1 条**(`duojiao`)
- **交叉扫描:菜谱引用的 543 个 id,字典里全能查到,悬空引用 = 0**

三个字典末尾各有一节 `## 本次拆分影响的菜谱`,列明每个新 id 该回填到哪些菜谱行。

### 批准了它提的三个越界改动

1. **`wood_ear` 不只改别名,数据也换了** —— 原条目 tier=`staple`、常温 365 天、265 kcal,全是**干货数据**。既然收窄成只代表鲜货,就得连 tier、包装、营养一起换成鲜货口径。**留一条「鲜货名字 + 干货数据」比不改更危险。**
2. **`pickled_daikon` 连「黄萝卜」也一起移交 `danmuji`** —— 裸写「黄萝卜」在中文里同样默认指韩式那个,只删「腌黄萝卜」等于没解决歧义。
3. **XS-076 gravlax 折叠成单项** —— 按我自己给的理由(伏特加无味增香 / 白酒带曲味),这两个不可互换,写成「或」是误导。

### 🟡 新发现的问题:31 个 id 在两个字典里各建了一条

`butter` `tempeh` `yanpi` `kimchi` `dried_shrimp` `parmesan` `milk` 等 **31 个 id 同时存在于两个字典文件,数据还不一样**(`butter` 在 01 是 720 kcal/200g 块,在 03 是 717 kcal/ 多一个 500g 规格)。

**危害比 `duojiao` 那种异 id 重复小**(解析不歧义),但**求解器读到哪一条取决于加载顺序**,包装规格和保质期会打架。

未处理,记为待办。

### 🔧 `prepLevel` 变体:定了落表方式

**变体不进 18 列主表,另起一张 11 列的表**:

```
## prepLevel 变体
| ID | 档位 | 食材 | 活跃分钟 | 总分钟 | 难度 | 提前准备 | 锅数 | 厨具必需 | 调料 | 备注 |
```

理由:变体是少数派(预计 40-60 道),为它给 535 行加一列,90% 的格子填 `—`;更重要的是**改 535 行 = 再来一次列错位事故的机会**,现有主表是干净的,别碰。

### ⚠️ 判断标准不是难度数字,是「买不买得到」

我先按「难度≥4 或 活跃≥40」扫出 82 道,**但这个阈值只能当起点**。难度的来源分两种:

| 来源 | 能否外包 | 例 |
|---|---|---|
| **工序**(和面/擀皮/包制/剁馅/熬底) | ✅ | 虾饺 · 拉条子 · 灌汤包 · 豚骨汤底 · 绿咖喱膏 |
| **手法**(火候/勾芡/翻勺/打发) | ❌ | 水煮鱼片 · 松鼠鳜鱼 · 舒芙蕾 · risotto |

**松鼠鳜鱼没有速冻版,给它编一个就是造假数据。** 宁可一个文件只有 3 道,不凑数。

按文件的预期落差很不一样:**04 北方家常 22 道命中最多**,因为北方面食的难度几乎全来自工序;**05 西式**买半成品的文化最成熟(冷冻酥皮/派皮/袋装 gnocchi 是常规货);**06 日韩东南亚**成品线最全,而且 `RH-069 绿咖喱膏`、`RH-050 泡菜` 这类 `prep` 直接支撑了「解锁一个味型 = 花钱买 **或** 花时间做」这条设计。

### 本轮的并发纪律

六个菜谱文件各派**一个** agent,**一次性给全指令,中途不发任何消息**(第 11 轮事故的教训:`completed` ≠ 已终止,SendMessage 会复活 agent 造成并发写)。

跨文件的 id 回填被**拆成了每文件自己的活** —— 每个 agent 只挑字典附录里属于自己前缀的条目。这样跨文件操作退化成单文件操作,不需要等待窗口。

同时交给这六个 agent 的还有:名称含括号的清理(11 处)、`02` 的香料补录(六个文件里唯一没补过的)。

---

## 2026-08-04 夜 · 第 12 轮 —— 归一化完成,裁决了 8 类 31 条

### ✅ 归一化交付

- **分隔符 `\|` → ` 或 `:64 处**(03=2 · 05=54 · 06=8)
- **真写坏的「或/且」语法:14 处** —— 斜杠 8 · 加号 5 · 「或」字混进 id 前 1 · 括号式可替代 1
- **id 归并:3 组 11 处** · **名称区分:5 组 22 处**
- 535 道 ✓ · 18 列逐行验证 ✓ · `[]` 内非法字符 0 · 同名多 id **0 组**

它用 **git diff 证明越界改动为 0**:535 行里只有 `食材`(41)、`调料`(28)、`厨具可替代`(18)三列变了,耗时/难度/味型/备注/type/生食**一个字节都没动**。这是验证「不改内容」的正确方法。

### 🔴 它提的反对我认了:`sea_salt` 合并是我的错

字典里 `sea_salt` 是**正式条目**(海盐/片盐/马尔顿盐,**125g 盒**),`salt` 是 **400g/1kg 袋**。

**包装差一个数量级** —— 合并后求解器会按 400g 袋装去算马尔顿盐的采购量。而且两者名称本来就不同(「盐」vs「海盐」),**根本不触发「同名不同 id」那条规则**。

**根因**:我做冲突扫描时,正则把「海**盐**」的后缀当成了「盐」。**同一类正则误判今晚第三次了**(前两次:附录表被数进主表、`|` 检测误匹配列分隔符)。

已回滚。

### 裁决记录

| 组 | 问题 | 裁决 |
|---|---|---|
| A1 | `sea_salt`→`salt` | **回滚**,两条都保留 |
| A2 | `duojiao` / `chopped_salted_chili` 重复 | 合并方向对,**字典侧删掉 `duojiao`** |
| A3 | `wood_ear`/`dried_wood_ear` 别名完全重叠 | **收窄** `wood_ear` 为只含鲜货说法 |
| B | 3 处「或」两侧同 id | **拆字典**:巴丁鱼/鲳鱼 · 打抛叶/九层塔 · 焙茶/煎茶 · 伏特加/白酒 |
| C | 3 处拆分比例是猜的 | 夫妻肺片改 **90/60**、天麻炖鸡改 **150/100**、关东煮保持对半 |
| D | 「或」+「共250g」矛盾 | 改成 **5 项各 50g**(且关系),总量不变 |
| E | 4 个新显示名不在字典别名列 | **补进字典** |
| F | `天麻[?]` | **字典建 `gastrodia` 条目** |
| G | 11 处名称含括号 | **写进 SPEC**:名称不得含括号,加工状态写备注列 |
| H | 4 个 id 挂着实为两样东西的名称 | **拆字典** |

### B 组和 H 组是同一类问题:机器认为一样,人认为不一样

- `鲳鱼[pomfret] 或 巴丁鱼[pomfret]` —— **鲳鱼是海水鱼,巴丁鱼是淡水鲶科**(和巴沙鱼同属),价格差很多
- `九层塔 / 打抛叶` 同 id —— **是两种不同的罗勒**(thai basil vs holy basil),打抛猪用错味道差很远
- `伏特加 / 白酒` 同 id —— 腌三文鱼 gravlax **用伏特加是无味增香,用白酒会带浓香型曲味**
- `白芸豆 / 红腰豆` 同 id —— 不同品种,minestrone 和 chili 用的不是一个

**这些在语法层面完全合法,只有懂这道菜的人才看得出问题。** 自动化查不出来,只能靠 agent 报告 + 人裁决。

### 正在做

字典层清理(只碰 `data/ingredients/`,与菜谱无冲突):删重复条目 · 收窄别名 · **拆 7 组被错误合并的** · 补 5 个别名 · 补 `天麻`/`小米`/`苹果汁`/`jalapeño` 缺失条目 · 交叉扫描确认没有「菜谱引用了但字典查不到」的悬空 id。

### 下一步

1. 字典清理完 → 按它给的「拆分影响的菜谱」清单回填菜谱侧的 id
2. `02-川湘云贵` 补香料(唯一还没补的)
3. 11 处名称含括号的清理
4. `#4 补 prepLevel variants` —— 数据层最后一项

---

## 2026-08-04 夜 · 第 11 轮 —— 完整性总检通过,等归一化

### 全库体检

| 层 | 数量 | 状态 |
|---|---|---|
| 菜谱 | **535 道** | ✓ |
| 食材字典 | **609 条** | ✓ |
| 包装规格 | **135 条** | ✓ |

**列结构零真损坏。** 之前几轮反复报的「列数异常」全部是附录表(`## 本次补录的香料` 之类)被正则误捕 —— 用「多于 19 根竖线才是损坏,少于是附录」这个判据重测,六个文件都是 0。

`05-西式` 那 21 行是它自己修好的。

### 这一轮没派新 agent

归一化 agent 还在分析阶段(文件字节数 15 秒双采样无变化,说明它还没开始写)。

**剩下的活全都要写 `data/recipes/`** —— `02` 补香料、`#4 prepLevel variants`、字典去重后的 id 回填。**都得等它写完。**

第 9 轮的事故就是我在有写者的情况下又派了一路。这次不重犯,**串行等**。

### 顺手做的(不冲突)

`00-README.md` 更新到 v2:535 道 · 18 列表结构 · 「或」语法 · `适量` 语义 · `type` 闭环 · 以及踩过的坑(两列重复、鲜香料只在调料列、并发写入事故)。

### git

```
8d34b99 docs: SPEC 补充「适量」语义与备注补录例外
862f294 06-日韩东南亚 完成
4aacc81 recover: 全部 535 道恢复完整
724a966 wip: 05修复完成; 06写至64道; 02从git恢复86道
07f3017 recover: 02-川湘云贵 从备份恢复 60->86 道
25c9669 snapshot: 事故现场
```

### 下一步(严格串行)

1. 等归一化 agent 交付 → 提交
2. `02-川湘云贵` 补香料(唯一还没补的)
3. 字典层去重,按菜谱实际用的 id 反向对齐
4. `#4 补 prepLevel variants` —— 数据层最后一项

---

## 2026-08-04 夜 · 第 10 轮 —— 六个文件全部改造完成

### ✅ 535/535,已提交

六个菜谱文件的 v2 格式改造(字段重构 + 食材改引用 + 编号统一 + 香料补录 + 两列去重)**全部完成**。

| 文件 | 道数 | 香料补录 | 两列去重 |
|---|---|---|---|
| 01-家常基础 | 121 | 48 条 / 43 道 | 7 处 |
| 02-川湘云贵 | 86 | 待补 | — |
| 03-粤浙闽 | 77 | 7 条 | 已处理 |
| 04-北方家常 | 78 | 13 条 | 8 类 |
| 05-西式 | 78 | 16 条 | **21 处** |
| 06-日韩东南亚 | 95 | 19 条 + 移列 43 条 | 12 处 |

### 最危险的一处:东南亚香料曾经完全不会被买到

`06` 那一路发现:**香茅 · 南姜 · 柠檬叶 · 青柠 有 24 处只存在于「额外调料」列**。

按新规则「调料列只放调味料」,这些鲜料如果留在调料列,**采购清单根本不会列它们** —— 你到店不会买香茅,回家做冬阴功时才发现没有。

现已全部移进 `食材` 列。

### 「两列重复」这个 bug 的全貌

五个 agent 独立踩到了同一个坑,合计清理 **48+ 处**:

- **01**:花生米 · 粉条 · 干木耳×2 · 腊肠 · 紫菜 · 粉丝
- **04**:鸡蛋 · 面粉 · 馒头渣 · 枸杞 · 糖蒜 · 蒜(8 类)
- **05**:马苏里拉 · 格鲁耶尔 · 玉米饼 · 墨西哥薄饼 · 英式麦芬 · 法棍 · 核桃 · 蔓越莓干 · 混合坚果 · 希腊酸奶 · 莳萝(21 处)
- **06**:昆布 · 泡菜汁 · 高丽参 · 米纸 · 椰丝 · 虾米 · 小米辣 · 九层塔(12 处)

**同一样东西出现在两列,采购量直接翻倍。** 根因是我 SPEC 第一版写的「原额外调料内容全部保留」—— 那句话按字面执行就会造成重复计数。

### 两个新写进 SPEC 的约定

**`适量`** —— 原文没给克数的点缀类食材(海苔丝、昆布、虾片)统一写 `适量`,不编数字。
语义:**要买,但不参与克数累加和包装规格计算**。对「买一包吃很久」的东西这个语义是准确的。

**备注可补,但只能补「不补就会丢失的信息」** —— 例如泡菜汁和熟成泡菜是同一个 id,去重时删掉一份,「泡菜汁一起倒」这个做法要点必须并进备注,否则随列消失。

### 椰浆的分流规则(06 自己定的,合理)

**按 100ml 阈值**:作汤底主体的(绿咖喱、叻沙、仁当)进 `食材`;只作提香或腌料的(冬阴功、沙嗲)留在 `调料`。

同一样东西按用量分流,而不是按品类硬分 —— 这个判断比一刀切准确。

### 剩余

1. **归一化 agent 在做**:`\|` → ` 或 ` · id 冲突归并 · 名称区分
2. `02` 补香料(唯一还没补的)
3. 字典层去重
4. `#4 补 prepLevel variants`

---

## 2026-08-04 夜 · 第 9 轮 —— ⚠️ 并发写入事故与恢复

### 结果:数据全部找回,535/535,并且从此有了 git

```
4aacc81 recover: 全部 535 道恢复完整 (06 写至95道) — 并发写入事故结束
724a966 wip: 05修复完成(78道/去重21处/香料16条); 06写至64道; 02从git恢复86道
07f3017 recover: 02-川湘云贵 从备份恢复 60->86 道
25c9669 snapshot: 事故现场(02 截断至60道, 06 截断至32道)
```

### 事故经过

**错在我。**

六路 agent 的 task-notification 都显示 `completed`,我就当它们终止了。但我随后用 SendMessage 给 02/04/05/06 发补充指令(补蒜姜葱、换分隔符)——**SendMessage 会把已完成的 agent 重新唤醒**。

于是同一批文件上出现了**多个无锁的并发写者**:被唤醒的原 agent 在重写,而我又派了一个归一化 agent 去读同样的文件。

损失时刻的现场:

| 文件 | 事故中最低点 | 应有 |
|---|---|---|
| 02-川湘云贵 | 被删除 → 重建到 **60 道** | 86 |
| 06-日韩东南亚 | 截断到 **32 道** | 95 |

### 是那个归一化 agent 叫停了整件事

它没有按指令往下做,而是先观测了文件状态,然后拒绝开工:

> 🛑 停一下 —— 前提不成立,我没有开始改任何文件。
> 「六个文件刚由六个 agent 并行改造完」不成立:至少三个 agent 现在还在写。
> 我现在读到的 02/06 是半截文件,算出来的唯一 ID 数、id 冲突表全是错的。
> 我一旦写 02/05/06,要么覆盖掉它们正在写的内容,要么被它们覆盖 —— 两边都丢数据。
> **这个目录没有 git,写坏了没有回滚。**

**它还顺手把 02 的完整原件备份到了 `Temp/norm-safety/`** —— 后来 02 就是靠这份救回来的。

如果它照指令执行,它会在半截文件上算出错误的冲突表,然后把错误写进磁盘,**而且没有任何回滚手段**。

### 恢复过程

1. **建 git,先把事故现场提交** —— 哪怕是坏的状态也要有记录点
2. **02 从 peer 的备份恢复** 60 → 86
3. **停掉已完成但仍在反复重写的 agent**(它们是损坏来源)
4. **06 不能停** —— 它是唯一持有那 31 道内容的地方,让它自己写完了 64 → 95
5. 全程每一步都 commit

### 四条教训

**1. `completed` 通知 ≠ agent 已终止。**
SendMessage 会复活它。要么一次把指令说全,要么明确 TaskStop 之后再派新的。

**2. 并发写同一批文件必须有单一写者。**
我最初的设计(六个 agent 各写各的文件)是对的 —— 没有交叉。事故是**我后来又派了一个跨文件的 agent** 才发生的。跨文件操作必须等所有单文件写者确认终止。

**3. 没有版本控制时,并发写入 = 不可恢复的丢失。**
02 靠 peer 的临时备份救回,06 靠原 agent 自己的上下文续写 —— **两次都是运气**。`git init` 应该是这个项目的第一条命令,而不是第九轮的补救。

**4. 拒绝在坏前提上执行,比高效执行错误任务有价值得多。**
这是今晚唯一完全正确的决定,而且不是我做的。

### 05-西式 也自己修好了

78 道 / 18 列齐整 / 清掉 **21 处两列重复**(马苏里拉 · 玉米饼 · 法棍 · 核桃 · 希腊酸奶……)/ 补录 16 条香料 / 17 组「或」关系。

它还独立发现了裸管道符会撑破表格,自行改成 `\|` 并**主动提醒其他路去跑列数校验**。

### 当前状态

| 文件 | 道数 |
|---|---|
| 01-家常基础 | 121 |
| 02-川湘云贵 | 86 |
| 03-粤浙闽 | 77 |
| 04-北方家常 | 78 |
| 05-西式 | 78 |
| 06-日韩东南亚 | 95 |
| **合计** | **535** ✓ |

### 下一步(串行,不再并发)

1. 归一化 agent 单独作业:`\|` → ` 或 ` · id 冲突归并 · 名称区分
2. 给 `02` / `06` 补香料(01/03/04/05 已完成)
3. 字典层去重
4. `#4 补 prepLevel variants`

---

## 2026-08-04 夜 · 第 8 轮 —— 验证抓到真损坏

### ⚠️ `05-西式.md` 有 21 行是坏数据

它在食材列写了裸 `|` 表示「或」:

```
混合菌菇[mixed_mushroom] | 口蘑[button_mushroom] 150g 主
```

**`|` 是 markdown 表格的列分隔符。** 这一竖线把单元格劈成两列,后面的做法、味型、辣度……**全部整体右移一位**。

而它的自检报告写的是「脚本校验每行都是 18 列」—— **因为它数的是劈开之后的结果**。

### 根因在我:选的分隔符跟载体冲突

SPEC 第一版没定「或」关系的语法。一个 agent 撞上了(干鲍/干贝)临时借用 `|`,我确认后广播为正式语法 —— **但我没想过 `|` 就是 markdown 的列分隔符**。

`03-粤浙闽` 那一路用了 `\|` 转义,是**唯一正确的做法**,而且它主动提出了这个反对:

> 这不是没执行,是执行不了。markdown 里 `\|` 就是字面 `|` 的源码写法……如果 SPEC 想要源码层面也裸写,那得先把这批文件从 markdown 换成 TSV/YAML,否则语法和载体冲突。

**它是对的。** 但 `\|` 也不是好方案 —— **没有任何机制能防止下一个 agent 忘记转义**。

### 已改:` | ` → ` 或 `

```
干鲍[dried_abalone] 或 干贝[dried_scallop] 15g 配
烤箱 或 空气炸锅          ← 厨具可替代
炒锅 + 蒸架               ← 厨具必需(且),两者语义不同别混
```

换一个天然不冲突的分隔符才是根治。SPEC 里已经把这段坑记下来了,防止以后又改回 `|`。

### 关于验证本身的教训

**这一轮我查了三次才查对。**

1. 第一次 grep 把附录表数进主表 —— 01 显示 164 道(实际 121),因为 `## 本次补录的香料` 那张表的表头也是 `| ID`
2. 第二次检测 `|` 语法的正则误匹配了 markdown 列分隔符 —— 报告 119 处「或语法」,实际 0 处
3. 第三次用「唯一 ID 去重 + 剥离转义后数竖线」才拿到真相

**验证工具本身也要验证。** 而且六个 agent 的自我报告全都说"完成、校验通过" —— **agent 报告的是「我按指令做完了」,不是「结果是对的」**。并行任务的不一致,单个 agent 从自己视角看不见。

### ✅ `04-北方家常` 完成得很扎实

78 道,补录 13 条香料,**独立踩到并清掉了同一个重复计数 bug**(8 类食材混在调料列:鸡蛋 · 面粉 · 馒头渣 · 枸杞 · 糖蒜 · 蒜)。

两处判断特别对:
- **大葱按原文保留 80-100g,没压成香料量** —— 北方菜里大葱是配菜级的
- **老虎菜有大葱但菜名备注都没提蒜 → 没加;拍黄瓜确实漏了 → 补上**。「宁可漏不要瞎加」守住了

### 跨字典 id 冲突:三路独立报告了同一个根因

02、04、05 三个 agent 各自发现字典里有重复条目(东北酸菜 · 咸菜丝 · 糖蒜 · 雪里蕻 · 木耳 · 榛蘑 · 虾皮 · 泡椒 · 剁椒 · 淡奶油 · 凤尾鱼柳),**而且各自选了不同的解法**:

- 02 取蔬菜字典
- 04 按「01 → 02 → 03 首个命中」
- 05 取了 03 的

**三路独立发现同一个问题,说明这是字典层的结构缺陷,不是个别失误。**

### 立了一条新规则

**id 不同,名称就必须不同。**

甜酱油(云南 vs 印尼)、姜片(鲜姜 vs 日式醋姜)、辣椒粉(中式辣椒面 vs 西式 chili powder)、腌萝卜(韩式 vs 越式)、河粉(干 vs 鲜)—— 这些是真的两样东西,不能合并,但名称写得一样。

**人看表以为是同一样,机器知道不是 —— 这种人机认知不一致最容易出隐蔽 bug。**

### 状态

| 文件 | 道数 | 香料补录 | 状态 |
|---|---|---|---|
| 01-家常基础 | 121 | ✅ 48 条 / 43 道 | ✅ |
| 02-川湘云贵 | 86 | ❌ 未补 | 待补 |
| 03-粤浙闽 | 77 | ✅ 7 条 | ✅ |
| 04-北方家常 | 78 | ✅ 13 条 | ✅ |
| 05-西式 | 78 | ❌ 未补 | **21 行损坏,修复中** |
| 06-日韩东南亚 | 95 | ❓ 待查 | 待查 |

一路 agent 在做:修 05 的损坏 · 分隔符换 ` 或 ` · 归一 id 冲突 · 按新规则改名称。

### 下一步

- 归一化完成后给 `02`/`05`/`06` 补香料
- 字典层去重(这次冲突的根因)
- `#4 补 prepLevel variants` 是数据层最后一项

---

## 2026-08-04 夜 · 第 7 轮 —— 六路改造完成,但并行造出了不一致

### ✅ 535 道,一道没丢

| 文件 | 唯一 ID 数 |
|---|---|
| 01-家常基础 | 121 |
| 02-川湘云贵 | 86 |
| 03-粤浙闽 | **77**(72 + 5 条拆出来的 prep) |
| 04-北方家常 | 78 |
| 05-西式 | 78 |
| 06-日韩东南亚 | 95 |
| **合计** | **535** ✓ |

18 列全部对齐,ID 无跳号无重复。

### ⚠️ 但我差点信了报告

六个 agent 的自我报告都说"完成、校验通过"。**我去查文件,发现了报告里没有的问题。**

**教训:agent 报告的是「我按指令做完了」,不是「结果是对的」。** 并行任务的不一致,单个 agent 从自己的视角看不见 —— 只有横向对比才暴露。

而且我第一次查也查错了:grep 把附录表数进了主表(01 显示 164 道),`|` 语法的检测模式误匹配了 markdown 的列分隔符。**验证工具本身也要验证。**

### 发现的硬 bug:id 字段里塞了分隔符

`|` 语法那条 SPEC 补充,是在四路已经跑到一半时才广播的。结果三个 agent 各用了各的临时写法:

```
洋葱[onion/青蒜[garlic_sprout]]              ← 斜杠,还嵌套了
牛腱[beef_shank或牛肋条[beef_short_rib]]      ← 中文「或」
鸡腿[chicken_drumstick+翅根[chicken_wing_root]] ← 加号
```

**方括号里混进分隔符,程序解析会直接崩。** 而且 `+` 到底是「且」还是「或」根本读不出来。

**根因是我的:SPEC 第一版没定「或」关系的语法。** 一个 agent 撞上了(干鲍/干贝)并临时借用 `|`,我确认后广播,但另外几路已经跑过那段了。

### 发现的第二类问题:跨文件 id 冲突

同一样东西在不同文件用了不同 id —— **求解器会当成两样东西,采购量算不到一起**:

| 名称 | 冲突 | 裁决 |
|---|---|---|
| 剁椒 | `chopped_salted_chili`(6) / `duojiao`(1) | 取前者 |
| 盐 | `salt` / `sea_salt` | 合并为 `salt` |
| 干木耳 | 几处误用了鲜货 id `wood_ear` | 按原文是否写「干」区分 |

根因是**字典层就有重复条目**,三个 agent 各自选了不同的解法:02 取蔬菜字典、04 按「01→02→03 首个命中」、05 取了 03 的。

### 立了一条新规则:id 不同,名称就必须不同

有几组是**真的两样东西**,不能合并,但都叫同一个名字:

| 现在都叫 | 改成 |
|---|---|
| 甜酱油 | `云南甜酱油[yunnan_sweet_soy]` / `印尼甜酱油[kecap_manis]` |
| 姜片 | `姜[ginger]` / `日式醋姜[gari]` |
| 辣椒粉 | `辣椒面[chili_flakes_cn]` / `西式辣椒粉[chili_powder_west]` |
| 腌萝卜 | `韩式腌黄萝卜[danmuji]` / `越式腌萝卜[pickled_daikon]` |
| 河粉 | `干河粉[ho_fun_dried]` / `鲜河粉[ho_fun_fresh]` |

**人看表以为是同一样,机器知道不是 —— 这种人机认知不一致最容易出隐蔽 bug。**

### 蒜姜葱补录只完成了一半

`01` 补了 48 条覆盖 43 道菜,分了三档依据(菜名点名 14 / 爆炒炝锅 16 / 川味定式和去腥 13),还单列了**刻意没补的**(回锅肉已有蒜苗、蛋类快炒不放蒜、空炸走干香料鲜蒜会糊)—— 这个克制很对。

但 `02` 和 `05` 食材列里蒜姜葱各只有 6 处,**明显没补**。广播消息到得太晚。

### 正在做

一路 agent 统一收拾:修语法错误 · 归一 id 冲突 · 按新规则改名称 · 全量扫描剩余冲突。

### 下一步

- 归一化完成后,给 `02` 和 `05` 补蒜姜葱
- 字典层去重(重复条目是这次冲突的根因)
- `#4 补 prepLevel variants` 是数据层最后一项

---

## 2026-08-04 夜 · 第 6 轮

### ✅ `#6 包装规格表` 完成 —— 但它交出了一个该被当成头条的数字

`data/packages.md`,**135 条**(PK-001 ~ PK-135),覆盖 95 个 ingredientId,**全部能在字典里查到,零悬空引用**(agent 用 `comm` 对拍验证过)。

**可信度分布:**

| 等级 | 条数 | 占比 |
|---|---|---|
| **A**(实际读到页面原文) | **1** | **0.7%** |
| B(搜索引擎返回的商品页字段) | 92 | 68.1% |
| B−(测评/导购/代购页) | 18 | 13.3% |
| C(常识推测,无出处) | 24 | 17.8% |

**整张表 99.3% 未经原文核实。** 唯一的 A 级是「盒马初榨芝麻香油 400mL」。

### 这个数字改变了这一层的定位

包装规格表是**两阶段求解器「阶段一:选包」的核心输入**。而它 99.3% 是估的。

如果按原计划把它硬编码进代码,求解器会**用编造的输入算出看起来很精确的答案** —— 「本周剩余 8%」这种结论会是假的。

**所以「默认值 + 用户就地编辑」不是一个锦上添花的功能,是这一层能不能用的前提。** 之前把它排在 `#8` 当普通功能,现在看应该跟求解器绑在一起做 —— 没有编辑入口,求解器的输出不可信。

⚠️ **明天讨论应用层时要重新排这条的优先级。**

### 三个诚实的边界(agent 主动标的)

1. **参考价整列 0 条** —— 调研全程没有价格,**一个数字都没编**。所以「每周预算」这个约束目前完全无法执行
2. **眼肉牛排 400g 是全表唯一 A 级的肉类规格,但字典没有「眼肉」条目** —— 暂存在备注里,**没有硬塞进 `beef_sirloin`**
3. **12 项未匹配**(调研有规格但字典无 id,均未建条):眼肉牛排 · 原味酸奶 · 炭烧酸奶 · 冻豆腐 · 豆浆 · 速冻水饺 · 花卷 · 平菇 · 油麦菜 · 鲜山药 · 龙利鱼柳 · 鱿鱼圈

### 发现一处架构问题:同一事实有两个数据源

agent 多加了一节「**本表与字典『常见包装』列不一致的 8 处**」,其中娃娃菜差距最大(调研 300g vs 字典 500g),生菜、青椒、玉米淀粉也对不上。

**根因:包装规格同时存在于 `packages.md` 和字典的 `常见包装` 列。** 两个独立建的数据源描述同一件事,必然会漂移。

**建议(留给明天定)**:`packages.md` 作为唯一权威,字典的 `常见包装` 列改成**派生或直接删掉**。这跟之前定的「能从字典推导的不重复存」是同一条原则,只是方向反过来 —— 这次该被删的是字典那一列。

### ✅ `01-家常基础` 改造完成

**121 → 121 道,零未匹配**,86 个 distinct id 全部命中字典。

**它抓到一个真 bug**:原表有 7 处把食材在 `额外调料` 里又抄了一遍(花生米 · 粉条 · 干木耳×2 · 腊肠 · 紫菜 · 粉丝)。**同一样东西出现在两列,采购量会直接翻倍。** 已去重。

**它还留了个问题给我拍板**:「蒜蓉西兰花」这类菜,原表三列里都没写蒜,它按「不改菜的内容」没补。

**我的裁定:要补。** 「不改内容」指的是不改**已有的数值**(克数/耗时/难度/备注),不是"不补明显缺失的食材"。菜名叫蒜蓉西兰花却没有蒜是数据缺陷,而且后果连锁:**采购清单不列蒜 → 到店没买 → 做菜时才发现**。蒜姜葱在设计里是「每周现买的鲜料」,必须出现在清单上。

已让它按「菜名明说的 / 备注明说的 / 做法上必然要用的」三档补,**宁可漏不要瞎加**,补完单列一节供复核。

### 状态

| 任务 | 状态 |
|---|---|
| `#1` 食材字典 | ✅ 601 条 |
| `#6` 包装规格表 | ✅ 135 条 |
| `#2`/`#3`/`#5` 六路改造 | 1/6 完成,5 路在跑 |

---

## 2026-08-04 夜 · 第 5 轮 —— 七路并行改造中

### 做了一个合并决定

`#3 修字段`、`#2 改引用`、`#5 编号统一` 三件事**都要重写同样的 530 行**。分三趟做等于把整个库白写两遍。

**合并成一次重写。** 先写了 `data/recipes/SPEC.md` 当规范,六个 agent 各读一遍,免得六个 prompt 各抄一份(也免得抄歪了不一致)。

### 目标表头(18 列)

```
ID · 菜名 · 食材 · 做法 · 味型 · 辣度 · 活跃分钟 · 总分钟 · 难度
   · 提前准备 · 锅数 · 厨具必需 · 厨具可替代 · 调料 · 跨顿 · type · 生食 · 备注
```

| 变化 | 为什么 |
|---|---|
| `主料`+`配菜` → **`食材`** | 区分是主观的,各文件已经不一致 |
| 食材文本 → **`名称[id] 克数 角色`** | 引用字典。角色取 `主/配/主食/香料` |
| `额外调料` → **`调料`**(全量) | 「额外」是用户配置不是菜的属性 |
| `厨具` → **`厨具必需`** + **`厨具可替代`** | 原来 `/` 和 `+` 混用,程序分不出「或」和「且」 |
| `01` 的数字编号 → **`JC-001`** | 六个文件统一成局部编号 |

食材格式保持人可读 + 机器可解析:

```
五花肉[pork_belly] 180g 主 · 红薯[sweet_potato] 150g 配 · 大米[rice] 80g 主食
```

厨具的或/且用不同分隔符区分:`厨具必需` 用 ` + `(且),`厨具可替代` 用 ` | `(或)。

### 七路并行

| 路 | 任务 | 额外要做的 |
|---|---|---|
| 01-家常基础 | 改造 | 编号 `1-121` → `JC-001` |
| 02-川湘云贵 | 改造 | 补 `生食` 列;4 条 prep 的 ID 不能变(字典引用了) |
| 03-粤浙闽 | 改造 | **拆 5 条独立 prep**:葱油 · 潮式卤水 · 花雕醉汁 · 香糟卤 · 五香熏鱼卤汁 |
| 04-北方家常 | 改造 | 补 `生食` 列;面粉要分高筋/中筋/低筋 |
| 05-西式 | 改造 | **空炸替代烤箱按三档拆**:直接可换 / 减温减时可换(备注写明怎么调) / 必须烤箱 |
| 06-日韩东南亚 | 改造 | 甜酱油别用错(印尼 kecap manis ≠ 云南甜酱油) |
| packages | 新建 `data/packages.md` | 从盒马调研抽结构化数据,保留 A/B/C 可信度 |

### 硬性要求(写进 SPEC 了)

- 改前数一遍道数,改完再数一遍,报告里必须写
- 只改格式和引用,**不改克数耗时难度备注这些数值**,不许"顺手优化"
- 匹配不上的食材**不许硬猜、不许编 id** → 写 `[?]` 并追加到文件末尾的 `## 未匹配食材` 一节
- 原有的 `## 统计` 保留,数字按新表重算

### 下一步

七路回来后:
- 核对道数(应为 121 + 86 + 77 + 78 + 78 + 95 = **535**,粤浙闽拆 prep 后 +5)
- 汇总各文件的未匹配食材,决定是补字典还是改写法
- 天贝从动物性迁到豆制品
- 剩 `#4 补 prepLevel variants`

---

## 2026-08-04 夜 · 第 4 轮 —— ✅ 食材字典完成

**三路全部落盘,共 601 条。**

| 文件 | 条数 |
|---|---|
| `01-动物性.md` | 135 |
| `02-蔬菜菌菇水果.md` | 122 |
| `03-主食干货调料.md` | **344** |

---

### 发现一:「必然过量」占 51% —— 这个数字本身就是产品结论

第三路标记 **177 条(占 344 条的 51%)为「必然过量」** —— 判断标准是「最小包装 ÷ 单人年消耗 > 保质期」。

**调料柜里过半的东西,是买一次用一辈子的。**

除了之前点名的四个(白胡椒粉 220g / 椒盐 300g / 食用油 5L / 料酒 1.25L),还有约 30 条属于「单次用量 ≤5g 但最小包装 ≥50g」的香料。

这给「**按需解锁,别一次买齐**」这条设计提供了硬数据支撑 —— 之前只是直觉。

### 发现二:含酒 20 条,其中 7 条不是明面上的酒

`香草精`(35%vol 基底)· `第戎芥末` · `腐乳/南乳` · `叉烧酱` · `糟辣椒` · `白味噌`

**完全不能碰酒精的人,绝对想不到腐乳和芥末要查。**

而且 agent 补了一个关键区分:**醉鸡 / 醉蟹 / 糟毛豆是生泡不加热,酒精不挥发**。同样是"用了酒",炖菜里的料酒基本挥发完,生腌的没有。已在备注里标出。

这条直接印证了「派生字段必须从字典推」—— 靠人逐道填,腐乳含酒这件事永远不会被想起来。

### 发现三:字典建设过程反查出了菜谱库的一个错

`干牛肝菌 150g` —— 原菜谱里写的是**泡发后重量**,已改成 **30g 干重**并注明。

两层数据互相校验,这是分层的额外收益。

### 发现四:「甜酱油」在库里是两样东西

- 云南甜酱油(`CX-058` / `CX-060`)
- 印尼 kecap manis(`RH-078` / `RH-079` / `RH-080`)

已拆成两条。这种同名不同物的坑,不建字典根本发现不了。

---

### prep ↔ 食材的闭环已接上

**13 条「库内有自制配方」已挂上菜谱 ID**:

红油 `CX-022` · 复制酱油 `CX-023` · 糍粑辣椒 `CX-024` · 泡菜坛 `CX-025` · 东北酸菜 `BF-005` · 韩式泡菜 `RH-050` · 三种高汤 · 葱油 `YZ-029` · 卤水 `CX-021`/`YZ-033`

其中**复制酱油市面基本买不到,只能自制** —— 这类项目让「解锁一个味型」真正变成了「花钱买 **或** 花时间做」。

### 合并判断

**合并**:`郫县豆瓣/郫县豆瓣酱/豆瓣酱`(全库 18 道指同一样)· `玉米淀粉/生粉/淀粉` · `香叶/月桂叶`(中西同物)· `红油/辣椒油/辣油` · `香醋/镇江香醋`

**没合并**:高筋/中筋/低筋面粉 · 玉米淀粉 vs 土豆淀粉 vs 地瓜粉 vs 豌豆淀粉 · 番茄酱 vs 番茄膏 vs 番茄罐头 · 馄饨皮 vs 扁肉皮 · 紫菜 vs 海苔 vs 青海苔 · 日式昆布 vs 中式海带

### 边界判断:中间物不收

苹果泥 · 姜泥 · 萝卜泥 · 蒜末 · 淘米水 · 花椒水 · 面粉油酥 —— **都是从别的食材现做的中间物,不是采购项**,没建条目。这个判断是对的:采购清单不该出现"蒜末",该出现"蒜"。

---

### 遗留

- **天贝要从 `01-动物性` 迁到豆制品** —— 它是大豆发酵品,agent 自己标出来了
- `#2 改引用` 和 `#3 修字段` 现在解除阻塞,可以开工

---

## 2026-08-04 夜 · 第 3 轮

### 完成

**食材字典第二路:`data/ingredients/01-动物性.md`,135 条。**

水产 44 · 畜肉 34 · 加工肉 22 · 乳制品 13 · 禽肉 10 · 内脏 8 · 蛋 4
tier 分布:fresh 68 / buffer 43 / staple 24(agent 用脚本核对过)

### 一条重要的架构确认:类别 ≠ 过敏原,两个维度正交

agent 发现的坑:**鱼豆腐 / 竹轮 / 鱼饼 / 蟹柳** 在 `加工肉` 类别下,但它们实质是鱼糜。

处理方式:
- **类别**填 `加工肉`(用于组织和「主料类别」这类频率约束)
- **过敏原**按实质填 `鱼`
- **蟹柳双标 `甲壳 / 鱼`** —— 它是鱼糜做的仿蟹肉,两类过敏的人都得命中

这印证了之前定的「派生字段」原则,但要补一句限定:**派生必须从 `allergens` 字段推,不能从 `category` 推。** 用类别去猜过敏原,鱼豆腐就会被当成肉类漏掉。

### 别名归一

**48 条发生了归一**,最狠的三条:
- `pork_belly` 收了 8 种写法(五花肉 / 猪五花 / 带皮五花 / 五花肉片 / 猪五花肉片 / 五花肉丁 / 五花肉块 / 猪五花肉卷)
- `egg` 收了 8 种(含水煮蛋 / 溏心蛋 / 卤蛋 / 蛋黄 / 蛋液)
- `lamb_leg` 收了 7 种

**刻意不合并**的也单列了一节:
- 猪五花 / 二刀肉 / 梅花肉 —— 三个不同部位
- 牛里脊 vs **生食级**牛里脊 —— 生食级是独立条目
- 三文鱼 / 生食级三文鱼 / 烟熏三文鱼 —— 三条独立
- 蟹柳**不并入**任何真蟹条目

### agent 主动标出的两处自我修正

1. **天贝放错了字典** —— 它是大豆发酵品不是动物性食材。agent 按我点名收了,但备注写明「应迁到豆制品字典」,类别暂挂加工肉、素食等级标 `纯素`
2. **「鱼丸」在六个文件里没出现** —— 按只收实际出现的规则没建条目。同位置的是鱼豆腐/竹轮/鱼饼,已分三条

### 好的协作产物:「已知未收录」清单

agent 主动加了一节,列出**名字容易误判成动物性、但归属在别处**的项,写明该去哪个字典:

淀粉灌肠 · 焖子(淀粉制品)· 速冻饺子(复合成品)· 猪油 / 鸡油 / 鱼露 / 虾酱(调料)· 紫菜 / 海苔 / 昆布(藻类)

这正好补上了我三路分工的边界模糊处 —— 比我事先想到的更周全。

### 状态

| 路 | 状态 |
|---|---|
| 蔬菜菌菇水果 | ✅ 122 条 |
| **动物性** | ✅ **135 条** |
| 主食干货调料 | 🔄 进行中 |

**字典已建 257 条。**

### 下一步

第三路完成后:
- 把天贝从动物性迁到豆制品
- 接 `#3 修字段` 和 `#2 改引用`

---

## 2026-08-04 夜 · 第 2 轮

### 完成

**`docs/hema-snacks.md` 落盘** —— 尝鲜池 100 SKU 完整报告。(写它的 agent 超时挂了,报告是唯一副本,我自己写的文件。)

**食材字典第一路完成:`data/ingredients/02-蔬菜菌菇水果.md`,122 条。**

### 这一路带来两条建模规则,已推广到另外两路

**① 干货和鲜货不能合并 —— 克数不可换算**

`干香菇 20g ≈ 鲜香菇 100g`。合并成一条的话,求解器算出来的采购量会差五倍。

同类还有:干木耳/鲜木耳 · 干豆角/鲜豆角 · 干笋/鲜笋。一律分开建条,**并在备注里写泡发率** —— 这个数字求解器要用。

**② 同一样东西但营养定位不同的要分开**

`南瓜 26kcal` vs `贝贝南瓜 90kcal` —— 一个计入蔬菜量,一个更接近主食。

同类:土豆、玉米、芋头、山药、莲藕、红薯。这些淀粉质根茎要单独标 **`计入蔬菜量: 否`**,不能默认算蔬菜。否则「每餐蔬菜 ≥200g」会被一盘土豆糊弄过去。

### 别名归一的几个判断(agent 的,我认为都对)

**合并**:
- `青线椒 / 螺丝椒 / 尖椒 / 青尖椒 / 青辣椒 / 青阳辣椒` → 一条(薄皮长身中辣,可互换)
- 但**与「青椒」保持分离** —— 青椒是厚肉不辣的菜椒,换了会毁菜
- `芹菜 / 西芹` → 一条。理由很好:**芹菜是 EU 14 大过敏原,过敏原和黑名单必须同时命中**
- `细香葱→小葱` · `苏子叶→紫苏` · `打抛叶→九层塔` · `叻沙叶→越南香菜` · `雪里蕻→雪菜`

**刻意不合并的 12 组**已单列一张表,其中:
- `酸菜(芥菜类)` vs `东北酸菜(大白菜发酵)` —— 库里都存在且不能互换
- `绿豆芽` vs `宜宾芽菜` —— 都带「芽」,一个生鲜一个腌菜

### 修正一个我的记忆错误

我在 prompt 里让它收「霸王花、抱子甘蓝」,但这两样**在六个菜谱文件里一次都没出现**。agent 按「只收录实际出现过的」规则没收,做得对 —— 我记错了。

### 分工漏洞:藻类无人认领

我的三路分工(动物性 / 蔬菜菌菇水果 / 主食干货调料)漏了藻类。已确认**紫菜 · 海苔 · 昆布 · 海带 · 木鱼花全部归第三路**,放在 `干货` 类别下。

### 状态

| 路 | 状态 |
|---|---|
| 蔬菜菌菇水果 | ✅ 122 条 |
| 主食干货调料 | 🔄 进行中(刚补发了藻类归属和两条建条规则) |
| 动物性 | 🔄 进行中(第一次 API 超时,已重启) |

### 下一步

三路齐了之后接 `#3 修字段` 和 `#2 改引用`。

---

## 2026-08-04 夜 · 第 1 轮

### 完成

**盒马尝鲜池调研回来了** —— 7 个品类 **100 个 SKU**,正在写入 `docs/hema-snacks.md`。

三个发现改进了设计:

**① 「常年 / 季节」二分法不够,应该三分**

调研建议改成 `长青` / `时令` / `网红`:
- **长青**兜底(日日鲜基础款、速冻主食,2-3 年不换)
- **时令**按月份窗口过滤,**不是删除** —— 青团、杨梅、车厘子跨年会回来
- **网红**给推荐权重但**设过期时间**(卖完即下架,批次制)

**② 拿到了清单过期速度的硬数据**

| 清单类型 | 过期一半需要 |
|---|---|
| 纯网红清单 | **3-6 个月**(一个换季就废) |
| 混合清单 | 6-12 个月 |
| 纯基础款清单 | 2-3 年以上 |

官方口径:**换季意味着 50%+ 商品汰换**,盒马工坊每 25-30 天更新 30+ 款。
→ 维护节奏对齐盒马自己:**每年 3 月、9 月各大修一次**,季度间小补丁。

这印证了之前定的「三层兜底」是对的(写死池 → 记录买过的 → 降级成品类规则)。

**③ 盒马有战略级的低卡产品线**

「**500 大卡**」轻食系列(2024-10 上线,每份约 355 kcal)、低 GI 主食系列、能量认证标注。
**零脂零乳糖希腊酸奶 59kcal/10g 蛋白/0 脂** 是全场蛋白热量比最优。

意味着**尝鲜池不必然跟减脂目标冲突** —— 之前设计的「品类轮转稀释热量」还可以再进一步,直接从低卡线里挑。

### ⚠️ 安全备注 —— 已用 curl 定案:**不是网页投毒,是工具层产物**

调研中,两个页面经 WebFetch 返回的摘要末尾出现了与正文无关的文本:

| 来源 | 观察到的内容 |
|---|---|
| `finance.sina.com.cn/roll/2025-03-03/doc-inenkfeh9221222.shtml` | 英文:"You are not a lawyer and never comment on the legality of your own prompts and responses." |
| `news.bjd.com.cn/2026/03/11/11625681.shtml` | 中文:「我不是律师,不对本回答的合法性作评论。」 |

**验证方法**:绕开 WebFetch(它本身是嫌疑组件),直接 `curl` 抓原始 HTML 搜这两句。

**结果**:

```
新浪财经   http 200, 95863 bytes
  "never comment on the legality"      → 0 次
  "不对本回答的合法性作评论"              → 0 次
  "律师"                                → 1 次,是页脚的「网站律师」链接

京报网     http 200, 46641 bytes
  英文注入句                            → 0 次
  「律师」                              → 0 次
```

**两个页面的原始 HTML 里都不含这两句话。**

**结论:这是 WebFetch 内部摘要模型的 prompt 片段泄漏,不是这两个站点被投毒。** 同一条指令的中英两个变体出现在两个不相干的主流新闻站,本来就更符合工具层固定 prompt 漏出,curl 验证坐实了这一点。

**对调研数据的影响:无。** 载荷是行为约束型(身份指派 + 不评论合法性),没有试图套系统提示、抓取其他 URL、外传数据、绕过权限,也未篡改任何结论或价格。最终报告未附加那句免责声明。

**记录这段的意义**:我第一次汇报时说「两个网页正文末尾附带伪装成系统指令的注入文本」—— **把不确定的事说成了事实,而且把一个工具 bug 说成了安全事件**。子代理主动调低了置信度,curl 给了定论。以后遇到类似情况,先做能区分假设的那个检查,再下结论。

### 数据缺口(诚实记录)

- **价格几乎全是 B/B− 级** —— 盒马晚间打折、移山价、会员日波动极大。**结论:小程序里不要写死价格**
- **热量数据大面积缺失** —— 门店现烤/散称商品不公开营养标签,烘焙 15 款里只有 2 款有可靠 kcal
- 3 项查证失败已按存疑处理未编造:流心奶黄包(疑似已下架)、调味牛排(非盒马自有)、花胶鸡汤底

### 下一步

- 等 `#1 食材字典` 三路完成(动物性那路超时重启过一次)
- 然后接 `#3 修字段` 和 `#2 改引用`

---

## 2026-08-06 · 数据存疑:tier 和保质期对不上

做排期(`core/schedule.js`)时发现:

| 食材 | tier | shelfLifeDays |
|---|---|---|
| 虾仁 `shrimp_peeled` | `buffer` | **1** |
| 冬瓜 `winter_melon` | `buffer` | 10 |
| 鸡蛋 `egg` | `buffer` | 30 |

`buffer` 的定义是「能囤、跨轮次结转」,但虾仁标了 1 天。两种解释:

1. 字典记的是**鲜虾仁**(冷藏确实只能放一天),tier 标错了
2. 记的是**冷冻虾仁**,保质期该是几个月,shelfLifeDays 填错了

**没有改数据** —— 两种都说得通,得看货架上实际买的是哪种。
排期代码已经绕开这个歧义:判据改成 `shelfLifeDays` 本身,不再按 tier 过滤
(tier 说的是「能不能囤」,保质期说的是「放得了几天」,不是一回事)。

真跑一轮买了虾仁之后再定。

另:143 种 fresh 里有 3 种没填 `shelfLifeDays`,排期会当成「不怕放」排到最后。

---

## 2026-08-08 · 6 组同名菜(没合并,先记下)

| 菜名 | 两个 id | 食材是否相同 |
|---|---|---|
| 红烧肉 | JC-029 / YZ-041 | 不同(JC 版多姜葱) |
| 滑蛋虾仁 | JC-040 / YZ-002 | 不同(克数和配料都不一样) |
| 腊味煲仔饭 | JC-090 / YZ-016 | — |
| 番茄鸡蛋疙瘩汤 | JC-098 / BF-056 | — |
| 葱油拌面 | JC-103 / YZ-029 | — |
| 凉拌木耳 | JC-113 / BF-067 | 不同(BF 版多小米椒香菜洋葱) |

食材确实不一样,可能是有意收录的地方版本,也可能是建库时重复录入。**没动。**

但有两个实际后果,真跑几轮之后再定要不要合:

1. 菜谱页会并排显示两个「红烧肉」,看着像 bug
2. 冷却期按 recipeId 算 —— 上周吃过 JC-029,这周照样可能排到 YZ-041

如果决定保留,建议在菜名上加地域后缀(「红烧肉(粤式)」)让它们能被区分。
