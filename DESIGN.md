# 架构设计

> 这份文档记录我自己的架构判断。参考过 `MahjongScorer2`,但只采纳有独立理由的部分 —— 那是个娱乐产品,这是个工具产品,核心张力不一样。

---

## 一、定位:这不是菜谱推荐 App

**它是一人食的食材流转管理器。菜谱是消耗库存的手段,不是目的。**

理由很实际:**推荐菜谱是零门槛的**。任何 LLM 三秒钟就能给你四个菜,这件事没有壁垒,做得再好也只是"另一个菜谱 App"。

真正稀缺的是**状态**:

- 你冰箱里现在有什么、还剩多少
- 哪些东西快过期了
- 那瓶油你按现在的速度三个月吃不完
- 这袋菠菜买了两次都剩一半

这些只能靠**持续记录**积累,是别人抄不走的。而且它直接对应用户最初的三个痛点里的两个(买多了浪费、买少了跑两趟)—— 选择困难反而是最容易解决的那个。

**这个定位决定了优先级:库存模块 > 求解器 > 菜谱库 > 统计。**

---

## 二、分层

```
data/     静态资料 —— 食材规格表、菜谱库、厨具调料候选池
state/    动态状态 —— 库存、历史记录、profile、反馈
core/     纯函数   —— 求解器、库存推演、统计聚合、营养计算
ui/       只做展示和事件绑定,零业务逻辑
```

**铁律:`core/` 不 import 任何 UI 框架。** 它的输入是普通对象,输出是普通对象。

这样带来三件事:能被 vitest 直接测 · 能在浏览器和小程序两端零改动复用 · 将来迁移时只有 `ui/` 需要重写。

### 迁移到小程序的成本分布

| 层 | 成本 |
|---|---|
| `data/` `core/` | **零** |
| `state/` | **一个文件**(Store 抽象层) |
| `ui/` | 要重写,但只有这一层 |

两个具体的坑:

**1. 数据文件用 `.js` 不用 `.json`**
`.json` 要 `fetch` 读,`file://` 双击打开会被 CORS 拦掉,得起本地服务器。写成 `var INGREDIENTS = [...]` 用 `<script src>` 加载,双击就能跑。迁小程序时每个文件末尾加一行 `module.exports = ...` 即可。

**2. 存储包一层**
```js
const Store = {
  get: k => JSON.parse(localStorage.getItem(k) || 'null'),
  set: (k, v) => localStorage.setItem(k, JSON.stringify(v)),
};
// 小程序版:wx.getStorageSync / wx.setStorageSync
```
业务代码全程只调 `Store.get/set`,不直接碰 `localStorage`。

---

## 三、数据模型

### Ingredient —— 食材(抽象概念)

```js
{
  id: 'chicken_breast',
  name: '鸡胸肉',
  category: 'protein',        // protein|leafy|root|mushroom|bean|egg|staple|seasoning
  tier: 'fresh',              // fresh|buffer|staple  ← 三级库存,决定策略
  shelfLifeDays: 3,           // 冷藏可食用天数(从买回家算,≠ 门店在架天数)
  freezable: true,
  frozenShelfLifeDays: 180,
  openedShelfLifeDays: null,  // 调料才有
  per100g: { kcal: 133, protein: 24, fat: 3, carb: 0 },
  blacklistTags: [],          // 内脏/苦瓜/秋葵等打标
}
```

### Package —— SKU(盒马实际卖的那个包装)

```js
{
  id: 'hema_rrx_chicken_breast_300',
  ingredientId: 'chicken_breast',
  vendor: 'hema',
  name: '盒马日日鲜 冰鲜鸡小胸',
  netWeight: 300, unit: 'g',
  sellMode: 'prepack',        // prepack|piece|bulk
  price: null,
  confidence: 'B',            // ← A/B/C 数据可信度直接进数据模型
}
```

**`confidence` 是刻意进入模型的。** 盒马调研里净含量可信、保质期几乎全是估计。把可信度当成一等字段,以后校准时能一眼看出哪些数字动过、哪些还没验证。

### Recipe —— 菜谱

```js
{
  id: 'beef_cabbage_rice',
  name: '牛肉包菜焖饭',

  type: 'dish',               // dish | prep | both   ← 见下文
  method: 'ricecooker',       // stirfry|airfry|steam|stew|ricecooker|boil|panfry|deepfry|roast|cold
  cuisine: 'home',
  flavor: ['咸鲜'],
  spicy: 0,                   // 0不辣 1微 2中 3重

  variants: [                 // ← 同一道菜的不同「起点」,见下文
    {
      prepLevel: 'scratch',
      ingredients: [                                    // ← 只存引用,不存自由文本
        { ingredientId: 'beef_brisket', grams: 180, role: 'main' },
        { ingredientId: 'cabbage',      grams: 200, role: 'main' },
        { ingredientId: 'rice',         grams: 80,  role: 'staple' },
      ],
      seasonings: [                                     // ← 全量,不是「额外」
        { ingredientId: 'salt' }, { ingredientId: 'light_soy' },
        { ingredientId: 'oyster_sauce' },
      ],
      activeMinutes: 15,      // 人必须在厨房动手的时间
      totalMinutes: 40,       // 到能吃的墙上时间(含焖/炖/发酵/冷藏)
      difficulty: 2,          // 1新手无脑 … 5有难度
      aheadOfTime: null,      // '腌30分钟' | '隔夜' | '泡米4小时' | null
    },
  ],

  equipmentRequired: ['ricecooker'],   // 必需
  equipmentAlt: [['oven', 'airfryer']], // 组内可互相替代 ← 见下文
  potsUsed: 1,                // 洗碗成本,一人做饭真正的劝退点
  fumeLevel: 1,               // 0无油烟 1低 2中 3爆炒级 ← 没抽油烟机的厨房是硬约束
  keepsOvernight: true,       // 能不能做一次吃两顿
  scalable: true,             // 能不能按份数缩放(整条鱼/半个蛋 = false)
  baseServings: 1,
  season: null,               // null=全年,或 [3,5] 表示 3-5 月 ← 春笋/青木瓜
  costTier: 3,                // 1很便宜 … 5很贵

  verified: false,            // 你实际做过没有
  logs: [],                   // 实测:{date, actualMinutes, actualGrams{}, rating, fullness}
}
```

#### 核心原则:能从食材字典推导的,不在菜谱上重复存

菜谱只存 `{ingredientId, grams, role}`。下面这些**全部是算出来的,不是填的**:

| 派生字段 | 从哪来 |
|---|---|
| 过敏原 | 各食材 `allergens` 的并集 |
| 有刺 | 各食材 `hasBones` 的或 |
| 含酒精 | 各食材(含调料)`hasAlcohol` 的或 |
| 生食 | 食材 role 里有 `raw` 或 method 为 `cold` 且含生鲜动物性食材 |
| 素食等级 | 各食材 `vegLevel` 的最严格值 |
| 主料类别 | role=main 的食材的 `category` |
| 营养(蛋白/热量/蔬菜量) | Σ(grams × 食材 per100g / 100) |
| 成本 | Σ 食材成本 |
| 「一锅出」 | `potsUsed === 1` |
| 「额外调料」 | `seasonings` 减去用户储物柜 |

**这条原则的价值:加一个新过滤维度 = 给字典加一个字段,而不是回头重填 530 道菜。**

反过来说,**必须逐道填的只有那些无法从食材推导的**:做法、味型、辣度、耗时、难度、锅数、油烟、跨顿、可缩放、prepLevel。就这十个。

#### `equipmentRequired` vs `equipmentAlt`

原来只有一个 `equipment` 数组,分隔符含义混乱 —— 西式那批写 `烤箱/空气炸锅` 是**或**,川湘写 `炒锅+蒸架` 是**且**,程序分不出来。「我没有烤箱」这个过滤会直接算错。

拆成两个字段:`equipmentRequired` 是且,`equipmentAlt` 里每个子数组内部是或。

而且替代不是二元的 —— 西式那批实测分了三档:**直接可换 5 道 / 减温减时可换 8 道 / 必须烤箱 10 道**。所以 `equipmentAlt` 的元素还要带一个 `note`,说明换了之后要不要调整参数。


#### `activeMinutes` vs `totalMinutes`

原本只有一个 `minutes`,不够用。红烧肉总共 70 分钟,其中 50 分钟在焖,人不用在厨房;电饭煲焖饭 40 分钟,活跃只有 10 分钟。**这两个数对排菜的意义完全不同** —— 一个是"我有多少时间",一个是"我愿意花多少精力"。

拆开之后马上就体现出价值,库里出现了这些:

| 菜 | 活跃 | 总 |
|---|---|---|
| 腊八蒜 | 15 分钟 | **7 天** |
| 东北酸菜(自腌) | 30 分钟 | **30 天** |
| 杭式酱鸭 | 30 分钟 | **25 小时** |
| 四川泡菜 | 20 分钟 | **3 天** |

只有一个字段的话,腊八蒜要么标 15(骗人)要么标 10080(吓跑人)。

#### `type`:不是所有条目都是"一顿饭"

腊八蒜、四川泡菜、东北酸菜、酱牛肉、炸酱、葱油、卤水 —— 这些产出的是**配料**,不是一餐。

```
'dish'   一顿饭的组成部分,排进餐次
'prep'   预制配料,做完进库存,供别的菜引用
'both'   酱牛肉这种 —— 切盘是菜,切丝是配料
```

`prep` 类走**另一条时间线**(今天做,N 天后可用),产出物进 PantryItem,可被别的菜的 `ingredients` 引用(炸酱 → 炸酱面;酸菜 → 酸菜白肉;卤水 → 卤蛋卤鸡翅)。

不加这个字段,求解器早晚会给你排出「周六晚餐:东北酸菜(需 30 天)」。

#### `variants` / `prepLevel`:同一道菜有多个「起点」

「猪肉白菜水饺」不是一道菜,是三个版本:

| prepLevel | 食材 | 活跃 | 难度 |
|---|---|---|---|
| `scratch` | 面粉 150g + 肉馅 150g + 白菜 200g | 60 | 4 |
| `assembled` | 饺子皮 + 肉馅 + 白菜 | 30 | 2 |
| `readymade` | 速冻饺子 250g | **5** | **1** |

注意**采购清单完全不同** —— 一个买面粉,一个买速冻饺子。所以食材必须挂在 variant 上,不能挂在菜上。

**这个字段直接对着「执行率」这个最大风险。** 你哪个周末不想动,系统不该把整道菜滤掉,而该给 `readymade` 版:广式虾饺 `scratch` 是难度 5、活跃 60 分钟的酒楼点心活,但速冻虾饺蒸 12 分钟就能吃。

不是每道菜都有三档:锅包肉没有成品版,手擀面有挂面顶替,虾饺有速冻的。per-dish。

这也让之前那个「是否接受净菜/半成品」的配置项从**开关**变成了**滑杆**。

### PantryItem —— 库存条目

```js
{
  id, ingredientId, packageId,
  amount: 300, unit: 'g',
  addedAt, openedAt, expiresAt,
  location: 'fridge',         // fridge|freezer|pantry
  source: 'purchase',         // purchase|leftover|split  (split=分装冷冻产生的)
}
```

---

## 四、库存模块(核心)

### 三级策略

不同类别的处理逻辑完全不同,不能一套走到底:

| tier | 内容 | 策略 |
|---|---|---|
| `fresh` | 叶菜 · 鲜肉 · 豆腐 · 活鲜 | **周末两天内清零**,硬约束,不进跨周结转 |
| `buffer` | 冷冻肉 · 冷冻虾仁 · 根茎 · 鸡蛋 · 干货 | 跨周结转,排菜时优先消耗 |
| `staple` | 调料 · 米面油 | 不进每周采购清单,只在快没了/临期时提醒 |

### 入库出库全自动

| 事件 | 触发 |
|---|---|
| **入库** | 采购清单勾"已买" → 按 `Package.netWeight` 自动建 PantryItem |
| **分装** | 采购清单上的"到家分装冷冻"勾选 → 一条拆成两条(fridge / freezer) |
| **出库** | 某顿点"完成" → 按 `Recipe.ingredients[].grams` 自动扣减,可改成实际称重值 |
| **开封** | 调料首次被某道菜用到 → 自动写 `openedAt` |

**零额外录入。** 如果要手动管库存,你三周就不用了 —— 所有库存变动必须搭在你本来就会做的操作上。

### 「按现在的速度吃不完」检测

```js
const weeklyRate = 近8周该食材总消耗 / 8;
const daysToFinish = amount / weeklyRate * 7;
const daysLeft = effectiveExpiry(item) - today;   // 开封了就用 openedShelfLifeDays

if (daysToFinish > daysLeft) → 标红 + 给建议
```

建议分三档:**多排用它的菜**(能救) / **下次买小包装**(有更小的 SKU) / **别买了**(最小档就已超年消耗)。

盒马调研已经产出一批第三档的名单 —— 白胡椒粉 220g、椒盐 300g 这类单人年消耗都吃不完、保质期只有 12 个月的,直接标"一次性购入,不进补货循环"。

---

## 五、求解器:两阶段,不是一步优化

这是我跟"先选菜再算食材"的常规做法分歧最大的地方。

**从选包出发,不是从选菜出发。** 因为规格是硬约束,菜是软的。

### 阶段一:选包(采购决策)

```
输入:目标顿数 N、profile、当前库存、预算
输出:一组 Package(采购清单)

1. 先扣掉现有库存能覆盖的部分,临期项加权(必须用掉)
2. 蛋白缺口 = N × 单顿蛋白量 − 库存    → 选 ~2 个蛋白包
3. 蔬菜缺口 = N × 单顿蔬菜量 − 库存    → 选 ~2-3 个蔬菜包
4. 预算/黑名单/保鲜期过滤
```

盒马的规格分布让这一步异常简单:

| 品类 | 最小包装 | 一人一顿 | = 几顿 |
|---|---|---|---|
| 冷鲜肉 | 300-400g | 150-200g | **2 顿** |
| 绿叶菜 | 300g/袋 | 150-200g | **2 顿** |
| 豆腐 | 350g | 150-200g | **2 顿** |
| 菌菇 | 125-200g | 100g | **2 顿** |

**包装规格天然就是"2 顿的量"**,所以 4 顿 ≈ 2 个蛋白包 + 2-3 个蔬菜包,候选组合只有几十种。

### 阶段二:排菜(给定包,找菜)

```
输入:阶段一选出的包、可做菜集
输出:N 道菜 + 排到哪一顿

硬约束:
  - 每个 fresh 包必须被完整消耗(剩余 < 10%)
  - 至少 ceil(N/2) 种不同 method(做法不重复)
  - 保鲜期排序:叶菜/鲜肉排前,冷冻/根茎排后
  - 单顿 minutes ≤ 上限,potsUsed ≤ 上限
  - 冷却期内不重复出现同一道菜
软目标(打分):
  - 剩余量最小
  - 评分期望最高
  - 恰好 1 道未做过的新菜
```

实现:随机采样 200 次 + 打分取最优。一人份搜索空间很小,够用,而且比精巧算法好调试十倍。

**为什么要分两阶段**:一步优化的空间是 `|SKU组合| × |菜组合|`,会爆炸。拆开之后阶段一只有几十种,阶段二在食材固定的前提下也只有几十种。

---

## 六、统计:诊断,不是颁奖

这里我跟自己之前的建议分歧最大,记录一下改了什么。

**麻将统计好玩是因为有对手和观众** —— 克星、苦主、谁是你爸爸,乐趣 100% 来自截图发到群里。**做饭没有对手,也没有观众。** 直接搬一套徽章过来,会做出一堆自己看两次就腻的东西。

所以形式要换:**从颁奖式换成诊断式。**

| 颁奖式(麻将适用) | 诊断式(这里适用) |
|---|---|
| 🏆 你获得「黑暗料理」称号 | 你打低分的菜里 73% 需要大火快炒 —— 可能不是手艺问题,是家用灶火力不够。建议多排空炸和焖煮 |
| 🏆 你获得「超时王」 | 你实际耗时平均比菜谱标注长 62%,已把耗时上限从 45 分钟收紧到 35 分钟 |
| 🏆 你获得「冰箱考古学家」 | 菠菜你连续 3 周都剩一半 —— 300g/袋对你是超量的,建议改买娃娃菜(可放 5-7 天) |

诊断式的每一条都**接一个动作**,而且大部分能自动执行。颁奖式只能被动观看。

### 三类统计

**A. 诊断类** —— 系统读出来的结论 + 给的建议
耗时 vs 评分相关性 · 低分菜共性 · 常剩食材 · 执行率下滑 · 营养缺口

**B. 自调类** —— 不给你看,直接改算法

| 观测 | 自动调整 |
|---|---|
| 执行率(排 4 顿做了几顿) | 连续两周只做 2 顿 → 自动降到 2 顿 |
| 实际/标注耗时比 | 系统性超时 → 收紧上限 |
| 某食材剩余率 | 总是剩 → 采购量下调或换小包装 |
| 缺货记录 | 常缺货 → 降权 |

**执行率是最重要的一个**,它直接对着"一时热度"这个风险。系统不该在你只做 2 顿时还固执排 4 顿让你有负罪感。

**C. 进度类** —— 让你看到自己在变好

不是"对抗外卖",是**看到自己变好**。这是单人工具真正的心理驱动:
学习曲线(同一道菜 50 分钟 → 28 分钟)· 浪费率下降曲线 · 体重曲线 × 做饭周 · 菜谱库成长(「已验证 14 道,能撑 3.5 周不重复」)· 省钱累计

### 两条铁律

1. **数据必须被动落下** —— 除了打分和称重,全部来自你本来就会做的操作
2. **每个指标背后要能接一个动作** —— 接不上的就是虚荣指标,删掉

### 称号系统:第一版不做

先跑几周,看诊断类和进度类够不够。称号是给**已经在用**的产品加粘性,不是让人开始用。真觉得缺乐趣再加,而且要从实际数据里长出来 —— 你连续三周剩菠菜,那才有资格出一个跟菠菜有关的梗。

---

## 七、配置层

```
Profile(长期)  ──┐
本周临时覆盖    ──┼──> 约束求解器
系统学到的      ──┘
```

**冷启动只问 5 个**:厨具勾选 · 食材黑名单 · 辣度 · 单顿耗时上限 · 每周预算。
其余 40 多项全给默认值,收进"高级设置"。

**让一个有选择困难症的人在做饭前先做 40 个决定,是把问题放大了。**

### 哪些不该让用户设

口味轻重(靠"太咸/太淡"反馈) · 实际耗时(自动记) · 菜系权重(靠评分涨落) · 黑名单增量(靠"难吃"累积)。

### ⚠️ 但饭量不能靠反馈学

原方案"用户说不够吃就加量"学到的是**你想吃多少**,不是**你该吃多少**。减脂目标下两者方向相反,会一路加上去,系统还以为自己在变准。

| | 来源 | 会不会漂移 |
|---|---|---|
| 该吃多少(热量/蛋白目标) | 公式算 | **不漂移,是锚** |
| 实际吃多少 | 称重 | 用来**发现偏差**,不改目标 |
| 怎么吃才饱 | "不够吃"反馈 | 只调**构成**,不调总量 |

"不够吃"的处理有护栏,热量上限始终是硬的:
```
1. 蛋白没到目标? → 加蛋白      (饱腹感最强)
2. 蔬菜 < 400g?  → 加蔬菜体积  (几乎不加热量)
3. 能量密度高?    → 换炖/蒸/汤 替代 炒/炸
4. 主食没到上限?  → 小幅 +15g 生重,最多两次
5. 都到顶了      → 不加量,改提示饱腹策略
```

营养目标用 Mifflin-St Jeor 算,体重存成时间序列(体重变 → TDEE 变 → 目标重算)。

---

## 八、关键决策记录

改过主意的都记下来,免得以后重复讨论。

| 决策 | 结论 | 理由 |
|---|---|---|
| 定位 | **食材流转管理器**,不是菜谱推荐器 | 推荐零门槛,库存状态才是护城河 |
| 不重复 vs 不浪费 | **主料复用、做法不重复** | 两者打架;且盒马包装规格 = 2 顿的量,这是唯一解 |
| 求解器 | **两阶段:先选包,再排菜** | 一步优化搜索空间爆炸;规格是硬约束,菜是软的 |
| 饭量参数 | **公式定目标,称重做监测,反馈只调构成** | ~~靠反馈学饭量~~ 在减脂目标下会一路漂移 |
| 统计形式 | **诊断式**,第一版不做称号 | ~~照搬麻将 85 个称号~~ 那是娱乐产品,有对手有观众;这里没有 |
| 心理驱动 | **看到自己变好**(进度) | ~~对抗外卖(对抗)~~ 也是在硬套麻将的对抗框架 |
| 技术路线 | 本地 HTML → 小程序 | 零审核零成本,试错快一个数量级 |
| 开工时机 | 先手工跑 3-4 周 | 现在没有真实克数/耗时/饭量,建库只能编 |
| 调料 | 按需解锁,先买 11 样 | 一人吃,全买齐 = 把浪费从冰箱搬到柜子里 |

## 九、从 MahjongScorer2 采纳的(每条都有独立理由)

- **纯函数聚合层** —— 通用好架构,不是麻将特有
- **实时全量计算,不预聚合** —— 这里数据量比麻将小一个量级,省掉缓存失效的复杂度
- **单 key JSON + 防御式 load**(字段级默认值、非法项 filter、catch 兜底)
- **导出 JSON 带 version + 智能引号/BOM 清洗** —— 数据不能丢
- **空状态写"下一步做什么"** —— 好的 UX 原则
- **录入类型自动推导** —— 反馈要压到 30 秒内,少一个选择器就少一次放弃
- **FEATURES.md 写"为什么不做"** —— 好习惯

**明确不采纳的**:85 个称号(见第六节) · JSONBin 云同步(配额天花板,一个人用不着) · 拍照识别(ROI 最低)

**要绕开的坑**:别写 1800 行单文件页面 · 写完的聚合函数记得接到 UI · **深色模式第一天就规划配色变量**,别硬编码 hex
